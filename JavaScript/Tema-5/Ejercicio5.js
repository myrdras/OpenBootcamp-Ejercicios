const altura = 163

const alturaM = altura / 100 // 1.63

const peso = 55.5

const alturaArriba = alturaM.toFixed(1) // 1.6

const pesoAbajo = Math.floor(peso) // 55


const valor = (Number.MAX_VALUE + 1) === Number.MAX_VALUE // true